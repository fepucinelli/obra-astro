services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: obra_dev
    volumes:
      - obra_db_data:/var/lib/postgresql/data

volumes:
  obra_db_data:
